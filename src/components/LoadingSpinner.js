import React from 'react';
import './LoadingSpinner';

const LoadingSpinner = () => {
    return (
        <div style={{
            textAlign: 'center'
        }}
        >
            Loading...
        </div>
    );
};

export default LoadingSpinner;